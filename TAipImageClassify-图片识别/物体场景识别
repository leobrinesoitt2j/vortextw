物体场景识别
===

> ###返回内容介绍
> https://ai.qq.com/doc/visionimgidy.shtml

### 物体识别-visionObjectr

```
public static String sample(TAipImageClassify client) throws Exception {
  // 参数为本地图片路径  图片格式 返回结果个数
  String imagePath = "./User/xiaoshaui/kj.jpg";
  String result = client.visionObjectr(imagePath,1,5);
  // 参数为本地图片二进制数组  图片格式 返回结果个数
  byte[] image = FileUtil.readFileByBytes(imagePath);
  String result = client.visionObjectr(image,1,5);
  return result;
}
```

### 场景识别-visionScener

```
public static String sample(TAipImageClassify client) throws Exception {
  // 参数为本地图片路径  图片格式 返回结果个数
  String imagePath = "./User/xiaoshaui/kj.jpg";
  String result = client.visionScener(imagePath,1,5);
  // 参数为本地图片二进制数组  图片格式 返回结果个数
  byte[] image = FileUtil.readFileByBytes(imagePath);
  String result = client.visionScener(image,1,5);
  return result;
}
```

