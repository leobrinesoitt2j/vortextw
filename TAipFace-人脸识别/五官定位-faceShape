五官定位-faceShape
===

> ###返回内容介绍：
> https://ai.qq.com/doc/faceshape.shtml

### 使用


```
public static String sample(TAipFace client) throws Exception {
  //参数为本地图片路径
  String imagePath = "./User/xiaoshaui/face.jpg";
  String result = client.faceShape(imagePath);
  //参数为本地图片二进制数组
  byte[] image = FileUtil.readFileByBytes(imagePath);
  String result = client.faceShape(image);
  return result;
}
```

