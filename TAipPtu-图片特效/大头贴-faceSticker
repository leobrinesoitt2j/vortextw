大头贴-faceSticker
===

> ###返回内容介绍
> https://ai.qq.com/doc/facesticker.shtml

### 使用

```
public static String sample(TAipPtu client) throws Exception {
	// 参数为本地图片路径
	String imagePath = "./User/xiaoshaui/kj.jpg";
	String result = client.faceSticker(imagePath);
	// 参数为本地图片二进制数组
	byte[] image = FileUtil.readFileByBytes(imagePath);
	String result = client.faceSticker(image);
	return result;
}
```

