图片滤镜（天天P图）-imgFilter
===

> ###返回内容介绍
> https://ai.qq.com/doc/ptuimgfilter.shtml

### 使用

```
public static String sample(TAipPtu client) throws Exception {
	// 参数为本地图片路径 滤镜特效编码
	String imagePath = "./User/xiaoshaui/kj.jpg";
	String result = client.imgFilter(imagePath,1);
	// 参数为本地图片二进制数组 滤镜特效编码
	byte[] image = FileUtil.readFileByBytes(imagePath);
	String result = client.imgFilter(image,1);
	return result;
}
```

